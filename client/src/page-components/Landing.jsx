import React, { Component } from 'react';

const Landing = () => (
  <div>
    <h1>Landing for now</h1>
  </div>
);

export default Landing;


// are we still using server-side rendering of login?
// <Route path='/login' component={Login} />